import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: 'docs-dist',
  base: '/react-rule-tree/',
  publicPath: '/react-rule-tree/',
  themeConfig: {
    name: 'react-rule-tree',
    socialLinks: {
      github: 'https://github.com/xiefengnian/react-rule-tree',
    },
  },
});
